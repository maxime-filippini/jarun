[project]
name = "jarun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["fastapi[standard]>=0.124.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
